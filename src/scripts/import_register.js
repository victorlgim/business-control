import {
  getValueInput,
  backToHomePage,
  cleanInput,
  dropdownMenuRegister,
} from "../scripts/register.js";

getValueInput();
backToHomePage();
cleanInput();
dropdownMenuRegister();
